# 本システムの目的および改修時のルール
- このシステムはNPBの2016年以降の記録については網羅的に把握している専門家AIチャットであり、原則として、2016～昨日時点までのデータはすべてDBに入っている前提である。
- 上記の理由から、その期間に一度でもNPBに在籍していた選手であれば必ずデータがあるのが正常な状態であり、ひとつでも歯抜けがある時点でNGとする。
- たとえば、メジャー移籍や引退等で今年NPBにいない選手であっても年を遡ればDBに存在するはずであるため、DBになければ正常ではなく、NGである。
- NPBに記録されている数値自体があとから変わることはないため、元DBにデータがすべて入っているのが確認できた時点で、修正においてもカラム追加・変更などの必要がない限りはやみくもにDBをいじらないこと（切り分けがしづらく不具合の温床になる）。
- つまり、テストでNGが生じた際の順番としては、ロジック修正に入る前にDBのデータがすべてそろっているかをまず確認→なければDBをまず完成させる→ロジックの修正、というのが正しく、ロジックを修正している最中にDBの歯抜けを発見してDBをいじるというのは不適切な対応である。
- ロジックやDBをいじった場合、テストとして必ず\docs\qa-test-cases.mdの質問をすべて実行し、本番挙動と\docs\qa-test-cases.mdのAに乖離がないようにQA記載内容を常に最新に保つこと。
- \docs\qa-test-cases.mdの質問を実行した際、「見つからない」「DBにない」「わからない」「できない」等で回答が終わるものがあった場合、専門家AIとして失格であるためその時点でNGとして、修正を行わなくてはならない。
- たとえば、ユーザーがXX年のなんらかのデータを求めたが指定年に情報がない場合、断りを入れたうえで代わりにデータのある直近年から探して答えるべきであり、「DBにない」で済ませるのはNG。
- たとえば、ユーザーが最近の特定選手の成績を求めたが一軍成績がない場合、断りを入れたうえでファーム成績を出すか、そもそもここ最近は試合自体に出ていないかなどを明確に答えるべきであり、「DBにない」で済ませるのはNG。
- たとえば、ユーザーが特定の指標について優れた選手を並べるよう求めた場合、回答する際には根拠と計算式などを明示しなくてはならず、もしその指標をあなたが知らなければ計算方法をユーザーに確認するべきであり、「わかりません」で済ませるのはNG。
- 上記の例はほんの一部にすぎず、一貫してあなたが取らなければいけない姿勢は、NPBに限定した専門家AIとして信頼できるとユーザーに思わせるふるまいである。
- 修正を行う際には、NGとなった質問のみに対しての小手先の対策ではなく、コードベース全体の仕様を把握したうえで影響範囲や拡張性なども考慮し齟齬が発生しないようベストプラクティスと思える形で慎重に対処すること。
- 修正中に\docs\qa-test-cases.md以外に必要と思われるテスト質問パターンが生じた場合、それを実行したうえで\docs\qa-test-cases.mdに必ず追記することを徹底し、精度改善の観点からも質問パターンは随時充実化させていくのが望ましい。
- ちなみに、選手はNPB公式で一意に番号が振られており（player_id）、これは球団が変わっても変更されることはないため、各年における選手の在籍情報などはこれをキーに引くことが可能である。
- player_idが不明でも基本的に同姓同名の選手は存在しないため、氏名からplayer_idを引くことが理論上可能である。